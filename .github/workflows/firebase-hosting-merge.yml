# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: add adminsdk json 
        env: 
          FIREBASE_ADMINSDK_JSON: ${{ secrets.FIREBASE_ADMINSDK_JSON }}
        run: |
          echo "$FIREBASE_ADMINSDK_JSON" > functions/api/kakao/today-house-cebe6-firebase-adminsdk-eqggo-21f028901a.json
 
      - name: Install functions npm packages
        run: |
          cd functions
          npm install
      - name:  Deploy functions to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only functions
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

